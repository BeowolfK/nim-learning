echo "Hello, World"
echo "What's your name? "
var name: string = readLine(stdin) ## name is a string
#[
    Double ## is for documentation purposes
    Single " is for single
    #[
        is for multiline comment
        and can be nested
    ]#
]#
echo "Hello, ", name